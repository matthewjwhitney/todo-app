import { TodoList } from "./components";

const App = () => (
  <main
    style={{
      display: "flex",
      justifyContent: "center"
    }}
  >
    <TodoList />
  </main>
);

export default App;
