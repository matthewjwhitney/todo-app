import { TodoList } from "../components";

const Main = () => (
  <main
    style={{
      display: "flex",
      justifyContent: "center",
    }}
  >
    <TodoList />
  </main>
);

export default Main;
